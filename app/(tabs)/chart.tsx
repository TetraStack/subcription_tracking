import React from "react";
import { Text, View } from "react-native";

const chart = () => {
    return (
        <View>
            <Text>chart</Text>
        </View>
    );
};

export default chart;